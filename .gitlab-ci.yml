stages: 
  - build
  - test

build front:
  image: node:16-alpine
  stage: build
  script:
    - yarn install
    - yarn build
    - echo "check index.html"
  artifacts:
    paths:
      - build

test front:
  image: alpine
  stage: test
  script:
    - test -f build/index.html

unit test:
  image: node:16-alpine
  stage: test
  script:
    - yarn test
